spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CNEvent
    username: root
    password: root
  jpa:
    properties.hibernate-dialect: org.hibernate.dialect.MySQLDialect
    hibernate.ddl-auto: update

server:
  port: 8081